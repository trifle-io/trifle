apiVersion: v1
kind: Service
metadata:
  name: {{ include "trifle.fullname" . }}
  labels:
    {{- include "trifle.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "trifle.selectorLabels" . | nindent 4 }}